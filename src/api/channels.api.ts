import { request } from './request';

export default {
    create: (values: any) => {

    },

    updateOne: (id: number, values: any) => {

    },

    deleteById: (id: number) => {

    },

    findById: (id: number) => {

    },

    findByQuery: (query: any) => {

    },
}